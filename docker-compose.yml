services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      DATABASE_FILE: kairizon.db
    volumes:
      - ./data:/app/data
      # SQLite DB is at ./data/kairizon.db on the host — back up by copying this file
