services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      DATABASE_URL: file:./data/kairizon.db
    volumes:
      - ./data:/app/data
